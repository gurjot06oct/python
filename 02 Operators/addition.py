# Integers
int_value = 10

# Boolean
bool_value = True

# Complex
complex_value = 3 + 4j

# Float
float_value = 3.14

# Tuple
tuple_value = (1, 2)

# String
string_value = "Hello"

# Dictionary
dict_value = {'a': 1, 'b': 2}

# Set
set_value = {1, 2}

# List
list_value = [1, 2]

# All combinations of addition operators
combinations = [
    (int_value + int_value),
    (int_value + bool_value),
    (int_value + complex_value),
    (int_value + float_value),
    # (int_value + tuple_value),
    # (int_value + string_value),
    # (int_value + dict_value),
    # (int_value + set_value),
    # (int_value + list_value),
    
    (bool_value + bool_value),
    (bool_value + complex_value),
    (bool_value + float_value),
    # (bool_value + tuple_value),
    # (bool_value + string_value),
    # (bool_value + dict_value),
    # (bool_value + set_value),
    # (bool_value + list_value),
    
    (complex_value + complex_value),
    (complex_value + float_value),
    # (complex_value + tuple_value),
    # (complex_value + string_value),
    # (complex_value + dict_value),
    # (complex_value + set_value),
    # (complex_value + list_value),
    
    (float_value + float_value),
    # (float_value + tuple_value),
    # (float_value + string_value),
    # (float_value + dict_value),
    # (float_value + set_value),
    # (float_value + list_value),
    
    (tuple_value + tuple_value),
    # (tuple_value + string_value),
    # (tuple_value + dict_value),
    # (tuple_value + set_value),
    # (tuple_value + list_value),
    
    (string_value + string_value),
    # (string_value + dict_value),
    # (string_value + set_value),
    # (string_value + list_value),
    
    # (dict_value + dict_value),
    # (dict_value + set_value),
    # (dict_value + list_value),
    
    # (set_value + set_value),
    # (set_value + list_value),
    
    (list_value + list_value)
]
