# Relational operators are mathematical, so expressions are converted to numeric form.
# In this example, True is converted to 1 instead of 5 being converted to True.
# Therefore, the result is False.

print(5 == True)  # False
